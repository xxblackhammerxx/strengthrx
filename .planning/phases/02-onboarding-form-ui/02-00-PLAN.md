---
phase: 02-onboarding-form-ui
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/int/onboarding-form.int.spec.ts
  - tests/e2e/onboarding.e2e.spec.ts
autonomous: true
requirements: [ONBRD-01, ONBRD-02, ONBRD-03, ONBRD-04, ONBRD-05, ONBRD-06, ONBRD-07, ONBRD-08, ONBRD-09, ONBRD-10]

must_haves:
  truths:
    - "Integration test file exists with failing tests for all onboarding form components"
    - "E2E test file exists with failing tests for page navigation and mobile responsiveness"
    - "@testing-library/user-event is installed as a dev dependency"
  artifacts:
    - path: "tests/int/onboarding-form.int.spec.ts"
      provides: "Failing-first integration tests for ONBRD-02 through ONBRD-09"
      min_lines: 50
    - path: "tests/e2e/onboarding.e2e.spec.ts"
      provides: "Failing-first e2e tests for ONBRD-01 and ONBRD-10"
      min_lines: 20
  key_links:
    - from: "tests/int/onboarding-form.int.spec.ts"
      to: "src/components/onboarding/"
      via: "imports onboarding components for render testing"
      pattern: "import.*from.*components/onboarding"
---

<objective>
Install @testing-library/user-event and create failing-first test scaffolds for all onboarding form requirements.

Purpose: Establish the test harness before implementation begins so that Plans 01 and 02 can verify their work against real test commands (`pnpm test:int`) rather than file-existence checks. Tests are written RED — they import components/pages that do not yet exist, so they will fail until implementation plans complete them.

Output: 2 test files + 1 new dev dependency installed.
</objective>

<execution_context>
@/Users/ericblackham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ericblackham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-form-ui/02-RESEARCH.md

@vitest.config.mts
@playwright.config.ts
@tests/int/clients-schema.int.spec.ts
@tests/e2e/frontend.e2e.spec.ts

<interfaces>
<!-- Target component interfaces (do not exist yet — tests will import these and fail RED) -->

From src/lib/schemas/onboarding.ts (Plan 01 creates):
```typescript
export const onboardingSchema: ZodObject<...>
export type OnboardingFormData = { goals: string[]; labsStatus: 'yes' | 'no'; firstName: string; lastName: string; email: string; phone?: string }
export const GOAL_OPTIONS: readonly { value: string; label: string }[]
export const STEP_FIELDS: Record<number, (keyof OnboardingFormData)[]>
export const STEP_LABELS: string[]
```

From src/components/onboarding/ (Plan 01 creates):
```typescript
export function StepIndicator({ currentStep }: { currentStep: number }): JSX.Element
export function StepGoals({ control, errors }: { control: Control<OnboardingFormData>; errors: FieldErrors<OnboardingFormData> }): JSX.Element
export function StepLabs({ control, errors }: { control: Control<OnboardingFormData>; errors: FieldErrors<OnboardingFormData> }): JSX.Element
export function StepContact({ register, errors }: { register: UseFormRegister<OnboardingFormData>; errors: FieldErrors<OnboardingFormData> }): JSX.Element
export function SuccessScreen(): JSX.Element
```

From src/components/onboarding/GetStartedForm.tsx (Plan 02 creates):
```typescript
export function GetStartedForm(): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @testing-library/user-event and create integration test scaffold</name>
  <files>tests/int/onboarding-form.int.spec.ts</files>
  <action>
1. Install the missing dev dependency:
```bash
pnpm add -D @testing-library/user-event
```

2. Create `tests/int/onboarding-form.int.spec.ts` with failing-first tests covering ONBRD-02 through ONBRD-09.

The test file should:
- Import `render`, `screen` from `@testing-library/react`
- Import `userEvent` from `@testing-library/user-event`
- Import `{ useForm }` from `react-hook-form` and `{ zodResolver }` from `@hookform/resolvers/zod`
- Import the onboarding components and schema (these DO NOT EXIST yet — tests will fail RED on import)

Write these test groups using `describe` and `it`:

**describe('StepGoals')** (ONBRD-02):
- it('renders all 6 goal option cards')
- it('toggles goal selection on click')

**describe('StepLabs')** (ONBRD-03):
- it('renders yes and no option cards')
- it('selects a labs option on click')

**describe('StepContact')** (ONBRD-04):
- it('renders first name, last name, email, and phone fields')

**describe('StepIndicator')** (ONBRD-05):
- it('highlights the active step')
- it('shows checkmark for completed steps')

**describe('GetStartedForm')** (ONBRD-06, ONBRD-07, ONBRD-08, ONBRD-09):
- it('preserves data when navigating back') (ONBRD-06)
- it('shows validation error when advancing without required fields') (ONBRD-07)
- it('shows loading spinner during submission') (ONBRD-08)
- it('shows success screen after submission') (ONBRD-09)

For step component tests that need react-hook-form context, create a simple wrapper component:
```typescript
function FormWrapper({ children }: { children: (form: UseFormReturn<OnboardingFormData>) => React.ReactNode }) {
  const form = useForm<OnboardingFormData>({
    resolver: zodResolver(onboardingSchema),
    defaultValues: { goals: [], labsStatus: undefined, firstName: '', lastName: '', email: '', phone: '' },
  })
  return <form>{children(form)}</form>
}
```

Each test should have a meaningful assertion that will FAIL because the components do not exist yet. This is intentional — the RED phase of TDD.

IMPORTANT: Do NOT stub or mock the components. Import the real paths. The tests must fail on missing module, confirming they target real code.
  </action>
  <verify>
    <automated>test -f tests/int/onboarding-form.int.spec.ts && grep -q "describe" tests/int/onboarding-form.int.spec.ts && grep -q "user-event" node_modules/@testing-library/user-event/package.json</automated>
  </verify>
  <done>Test file exists with failing tests for ONBRD-02 through ONBRD-09. @testing-library/user-event is installed. Running `pnpm test:int` fails RED (expected — components not yet created).</done>
</task>

<task type="auto">
  <name>Task 2: Create e2e test scaffold for page navigation and mobile</name>
  <files>tests/e2e/onboarding.e2e.spec.ts</files>
  <action>
Create `tests/e2e/onboarding.e2e.spec.ts` with failing-first Playwright tests covering ONBRD-01 and ONBRD-10.

The test file should:
- Import `{ test, expect }` from `@playwright/test`

**describe('Onboarding flow')** (ONBRD-01):
- test('homepage Get Started button navigates to /get-started') — clicks CTA, expects URL to contain '/get-started'
- test('/get-started page renders the onboarding form') — navigates to /get-started, expects heading or form element visible

**describe('Mobile responsiveness')** (ONBRD-10):
- test('form is usable at 375px viewport width') — sets viewport to 375x812 (iPhone SE), navigates to /get-started, expects form visible with no horizontal overflow

These tests will fail because the /get-started page does not exist yet. This is the expected RED state.

Follow the existing pattern in `tests/e2e/frontend.e2e.spec.ts` for Playwright conventions used in this project.
  </action>
  <verify>
    <automated>test -f tests/e2e/onboarding.e2e.spec.ts && grep -q "test(" tests/e2e/onboarding.e2e.spec.ts && grep -q "get-started" tests/e2e/onboarding.e2e.spec.ts</automated>
  </verify>
  <done>E2E test file exists with failing tests for ONBRD-01 (page navigation) and ONBRD-10 (mobile responsiveness). Running `pnpm test:e2e` fails RED (expected — page not yet created).</done>
</task>

</tasks>

<verification>
Both test files exist and contain meaningful test definitions:
- `grep -c "it\|test(" tests/int/onboarding-form.int.spec.ts` returns 10+ (one per behavior)
- `grep -c "test(" tests/e2e/onboarding.e2e.spec.ts` returns 3+
- `pnpm test:int` and `pnpm test:e2e` both fail RED (components/pages not yet created)
- `@testing-library/user-event` appears in package.json devDependencies
</verification>

<success_criteria>
- @testing-library/user-event installed as devDependency
- Integration test file covers ONBRD-02 through ONBRD-09 with specific assertions
- E2E test file covers ONBRD-01 and ONBRD-10
- All tests fail RED (missing imports/pages) — this is correct behavior for Wave 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-form-ui/02-00-SUMMARY.md`
</output>
